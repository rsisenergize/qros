name: 🚀 Qros Multi-Environment CI/CD (Auto Laravel Root Detection)

on:
  push:
    branches:
      - main

jobs:
  # ===========================================================
  # 1️⃣ BUILD JOB
  # ===========================================================
  build:
    name: 🧱 Build Qros
    runs-on: ubuntu-latest

    steps:
      - name: 🧩 Checkout code
        uses: actions/checkout@v4

      - name: ⚙️ Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: mbstring, bcmath, intl, mysql, gd, exif, pcntl

      - name: 📦 Install Composer Dependencies (Auto-detect Laravel Root)
        run: |
          # Find the Laravel root (folder containing artisan)
          ARTISAN_PATH=$(find . -type f -name artisan | head -n 1 | xargs dirname)
          echo "📂 Laravel root detected at: $ARTISAN_PATH"

          if [ -z "$ARTISAN_PATH" ]; then
            echo "❌ Could not find artisan file. Please check your repository structure."
            exit 1
          fi

          cd "$ARTISAN_PATH"
          mkdir -p bootstrap/cache
          chmod -R 777 bootstrap/cache
          composer install --no-dev --optimize-autoloader

      - name: 🧰 Build Frontend (if exists)
        run: |
          ARTISAN_PATH=$(find . -type f -name artisan | head -n 1 | xargs dirname)
          cd "$ARTISAN_PATH"
          if [ -f package.json ]; then
            npm install
            npm run build || npm run prod || echo "⚠️ No npm build step found"
          else
            echo "ℹ️ No frontend detected, skipping npm build."
          fi

      - name: 📁 Prepare Deployment Package
        run: |
          echo "📦 Detecting Laravel root for packaging..."
          ARTISAN_PATH=$(find . -type f -name artisan | head -n 1 | xargs dirname)
          echo "📂 Laravel root: $ARTISAN_PATH"

          if [ -z "$ARTISAN_PATH" ]; then
            echo "❌ Could not find artisan. Aborting build."
            exit 1
          fi

          mkdir -p deploy
          echo "🚚 Copying Laravel project to deploy folder..."
          rsync -av --progress "$ARTISAN_PATH/" ./deploy/ \
            --exclude='.git*' \
            --exclude='node_modules' \
            --exclude='tests' \
            --exclude='.env*' \
            --exclude='storage/app/public/*' \
            --exclude='storage/logs/*' \
            --exclude='storage/framework/*' \
            --exclude='public/uploads/*'

      - name: 🔍 Confirm deploy folder before upload
        run: |
          echo "=== Deploy folder contents (after rsync) ==="
          ls -la deploy | head -n 50
          if [ -f deploy/artisan ]; then echo "✅ Artisan found in deploy"; else echo "❌ Artisan missing"; fi

      - name: 📤 Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: qros_build
          path: deploy/

  # ===========================================================
  # 🔄 DEPLOYMENT JOB TEMPLATE
  # ===========================================================
  # Same logic reused for Dev, Test, Preprod, and Prod.
  # ===========================================================

  deploy-dev:
    name: 🌱 Deploy to Dev
    runs-on: ubuntu-latest
    needs: build
    environment: dev

    steps:
      - uses: actions/download-artifact@v4
        with:
          name: qros_build
          path: build/

      - name: 🧩 Checkout code
        uses: actions/checkout@v4

      - name: 🔍 Inspect build contents
        run: |
          echo "=== Workspace after artifact download ==="
          ls -la
          echo "=== Build directory ==="
          if [ -d build ]; then ls -la build | head -n 50; else echo "❌ build folder missing"; fi

      - name: 🔐 Copy Dev Environment File
        run: |
          mkdir -p build
          if [ -f .env.dev ]; then
            cp .env.dev build/.env && echo "✅ Copied .env.dev"
          else
            echo "⚠️ .env.dev not found in repo!"
          fi

      - name: 🚀 Upload via SSH (SCP)
        if: success() && (hashFiles('build/**') != '')
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "build/."
          target: ${{ secrets.DEPLOY_PATH_DEV }}

      - name: 🧠 Run Laravel Commands (Dev)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd ${{ secrets.DEPLOY_PATH_DEV }} || exit 1
            chmod -R 775 storage bootstrap/cache || true
            php artisan storage:link || true
            php artisan migrate --force || (php artisan migrate:rollback && exit 1)
            php artisan optimize:clear
            php artisan config:cache
            php artisan route:cache
            php artisan view:cache

  deploy-test:
    name: 🧪 Deploy to Test
    runs-on: ubuntu-latest
    needs: deploy-dev
    environment:
      name: test
      url: https://test.qros.in

    steps:
      - uses: actions/download-artifact@v4
        with:
          name: qros_build
          path: build/

      - name: 🧩 Checkout code
        uses: actions/checkout@v4

      - name: 🔐 Copy Test Environment File
        run: |
          mkdir -p build
          if [ -f .env.test ]; then
            cp .env.test build/.env && echo "✅ Copied .env.test"
          else
            echo "⚠️ .env.test not found in repo!"
          fi

      - name: 🚀 Upload via SSH (SCP)
        if: success() && (hashFiles('build/**') != '')
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "build/."
          target: ${{ secrets.DEPLOY_PATH_TEST }}

      - name: 🧠 Run Laravel Commands (Test)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd ${{ secrets.DEPLOY_PATH_TEST }} || exit 1
            chmod -R 775 storage bootstrap/cache || true
            php artisan storage:link || true
            php artisan migrate --force || (php artisan migrate:rollback && exit 1)
            php artisan optimize:clear
            php artisan config:cache
            php artisan route:cache
            php artisan view:cache

  deploy-preprod:
    name: 🚀 Deploy to Preprod
    runs-on: ubuntu-latest
    needs: deploy-test
    environment:
      name: preprod
      url: https://preprod.qros.in

    steps:
      - uses: actions/download-artifact@v4
        with:
          name: qros_build
          path: build/

      - name: 🧩 Checkout code
        uses: actions/checkout@v4

      - name: 🔐 Copy Preprod Environment File
        run: |
          mkdir -p build
          if [ -f .env.preprod ]; then
            cp .env.preprod build/.env && echo "✅ Copied .env.preprod"
          else
            echo "⚠️ .env.preprod not found in repo!"
          fi

      - name: 🚀 Upload via SSH (SCP)
        if: success() && (hashFiles('build/**') != '')
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "build/."
          target: ${{ secrets.DEPLOY_PATH_PREPROD }}

      - name: 🧠 Run Laravel Commands (Preprod)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd ${{ secrets.DEPLOY_PATH_PREPROD }} || exit 1
            chmod -R 775 storage bootstrap/cache || true
            php artisan storage:link || true
            php artisan migrate --force || (php artisan migrate:rollback && exit 1)
            php artisan optimize:clear
            php artisan config:cache
            php artisan route:cache
            php artisan view:cache

  deploy-prod:
    name: 🏁 Deploy to Production
    runs-on: ubuntu-latest
    needs: deploy-preprod
    environment:
      name: production
      url: https://qros.in

    steps:
      - uses: actions/download-artifact@v4
        with:
          name: qros_build
          path: build/

      - name: 🧩 Checkout code
        uses: actions/checkout@v4

      - name: 🔐 Copy Production Environment File
        run: |
          mkdir -p build
          if [ -f .env.prod ]; then
            cp .env.prod build/.env && echo "✅ Copied .env.prod"
          else
            echo "⚠️ .env.prod not found in repo!"
          fi

      - name: 🚀 Upload via SSH (SCP)
        if: success() && (hashFiles('build/**') != '')
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "build/."
          target: ${{ secrets.DEPLOY_PATH_PROD }}

      - name: 🧠 Run Laravel Commands (Production)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd ${{ secrets.DEPLOY_PATH_PROD }} || exit 1
            chmod -R 775 storage bootstrap/cache || true
            php artisan storage:link || true
            php artisan migrate --force || (php artisan migrate:rollback && exit 1)
            php artisan optimize:clear
            php artisan config:cache
            php artisan route:cache
            php artisan view:cache
