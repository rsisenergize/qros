name: 🚀 Qros Multi-Environment CI/CD (SCP Deployment for cPanel)

on:
  push:
    branches:
      - main

jobs:
  build:
    name: 🧱 Build Qros
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: ⚙️ Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: mbstring, bcmath, intl, mysql, gd, exif, pcntl

      - name: 📦 Install Composer Dependencies
        run: |
          mkdir -p bootstrap/cache
          chmod -R 777 bootstrap/cache
          composer install --no-dev --optimize-autoloader

      - name: 🧰 Build Frontend
        run: |
          if [ -f package.json ]; then
            npm install
            npm run build || npm run prod || echo "⚠️ No npm build step found"
          else
            echo "ℹ️ No frontend detected, skipping."
          fi

      - name: 📁 Prepare Deployment Package
        run: |
          mkdir -p deploy
          rsync -r --progress . deploy \
            --exclude=".git*" \
            --exclude="node_modules" \
            --exclude="tests" \
            --exclude=".env*" \
            --exclude="storage/app/public/*" \
            --exclude="storage/logs/*" \
            --exclude="storage/framework/*" \
            --exclude="public/uploads/*"

      - name: 📤 Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: qros_build
          path: deploy/

# ===========================================================
# Deployment Jobs
# ===========================================================

  deploy-dev:
    name: 🌱 Deploy to Dev
    runs-on: ubuntu-latest
    needs: build
    environment: dev
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: qros_build
          path: deploy/
      - uses: actions/checkout@v4
      - name: Deploy via Composite Action
        uses: ./.github/actions/deploy-composite
        with:
          env-name: dev
          env-file: .env.dev
          ssh-host: ${{ secrets.SSH_HOST }}
          ssh-user: ${{ secrets.SSH_USER }}
          ssh-key: ${{ secrets.SSH_PRIVATE_KEY }}
          deploy-path: ${{ secrets.DEPLOY_PATH_DEV }}

  deploy-test:
    name: 🧪 Deploy to Test
    runs-on: ubuntu-latest
    needs: deploy-dev
    environment:
      name: test
      url: https://test.qros.in
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: qros_build
          path: deploy/
      - uses: actions/checkout@v4
      - name: Deploy via Composite Action
        uses: ./.github/actions/deploy-composite
        with:
          env-name: test
          env-file: .env.test
          ssh-host: ${{ secrets.SSH_HOST }}
          ssh-user: ${{ secrets.SSH_USER }}
          ssh-key: ${{ secrets.SSH_PRIVATE_KEY }}
          deploy-path: ${{ secrets.DEPLOY_PATH_TEST }}

  deploy-preprod:
    name: 🚀 Deploy to Preprod
    runs-on: ubuntu-latest
    needs: deploy-test
    environment:
      name: preprod
      url: https://preprod.qros.in
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: qros_build
          path: deploy/
      - uses: actions/checkout@v4
      - name: Deploy via Composite Action
        uses: ./.github/actions/deploy-composite
        with:
          env-name: preprod
          env-file: .env.preprod
          ssh-host: ${{ secrets.SSH_HOST }}
          ssh-user: ${{ secrets.SSH_USER }}
          ssh-key: ${{ secrets.SSH_PRIVATE_KEY }}
          deploy-path: ${{ secrets.DEPLOY_PATH_PREPROD }}

  deploy-prod:
    name: 🏁 Deploy to Production
    runs-on: ubuntu-latest
    needs: deploy-preprod
    environment:
      name: production
      url: https://qros.in
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: qros_build
          path: deploy/
      - uses: actions/checkout@v4
      - name: Deploy via Composite Action
        uses: ./.github/actions/deploy-composite
        with:
          env-name: prod
          env-file: .env.prod
          ssh-host: ${{ secrets.SSH_HOST }}
          ssh-user: ${{ secrets.SSH_USER }}
          ssh-key: ${{ secrets.SSH_PRIVATE_KEY }}
          deploy-path: ${{ secrets.DEPLOY_PATH_PROD }}
